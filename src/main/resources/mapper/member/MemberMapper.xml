<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.feedapi.member.mapper.MemberMapper">

    <select id="getIdCnt" parameterType="String" resultType="Integer">
        SELECT COUNT(USER_ID) AS CNT
        FROM MEMBER
        WHERE USER_ID = #{userId}
    </select>

    <insert id="addMember" parameterType="org.feedapi.member.dto.JoinDTO">
        INSERT INTO MEMBER (USER_ID, USER_PASSWORD, EMAIL, ADDR)
        VALUES (#{userId}, #{userPassword}, #{email}, #{addr})
    </insert>

</mapper>